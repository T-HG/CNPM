PORT=5055
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change-this-secret
JWT_EXPIRES_IN=1d
DATABASE_FILE=backend/database/pharmacy.sqlite

# Frontend (tùy chọn — dev mặc định /api qua proxy Vite → :5055)
# Vẫn có thể dùng: VITE_API_URL=http://localhost:5055 hoặc http://localhost:5055/api (tự thêm /api)
# VITE_API_URL=http://localhost:5055/api
